[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "customer-support-ticket-intelligence-platform"
version = "0.1.0"
description = "Customer Support Ticket Intelligence Platform"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []
